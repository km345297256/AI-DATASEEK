# 2026-09-11 本机统一发布

状态：**2026-09-11 已完成本机统一部署，最终合并版本回归与部署后接口验收通过。**

入口：[可视化插件管理](http://localhost:7001/plugins?tab=renderers)。正式 API 返回 `engine=cordis`、74 个插件；已用实际浏览器确认目录加载完成。

用户已授权协调以下三个任务并统一部署，避免同一工作区的并行构建和重启覆盖彼此：

1. SciDB 领域第五批：SQLite 只读表格、ODIM 雷达、UGRID 海洋网格。
2. main 可视化迁移：矩阵工作台、天文工作台、比对浏览器、序列浏览器、基因组轨道、BLAST 命中，共六个独立 Cordis 插件。
3. 分析链路修复：正文伪工具调用纠正、执行结果交付检查、缺失成果安全补全，以及失败原因与最终正文保持一致。

合并源目录为 74 个插件、73 个批准适配器。保留已有插件 ID、用户启停偏好、AgentLoop/PlanActFlow/SSE、FastAPI 主机和 Docker 数据只读隔离。新增适配器均有受限格式边界，不等于完整上游桌面工具箱。

## 发布约束

- 使用既有 `./run.sh` 与 `ai-dataseek` Compose 项目，前端仍仅 `127.0.0.1:7001`，不新增公网入口或第二套服务。
- 三方生产代码冻结后再运行统一检查和构建；发布前再次检查运行中/等待中的会话及排队/运行/取消中的分析作业。
- 仅更新前端、后端和供新任务使用的沙箱镜像；不重建 MongoDB、Redis、MinIO、Office，不重启已有任务容器，不做数据库迁移或清库。
- 发布前前后端运行镜像以及发布前沙箱镜像保留 `pre-unified-20260911` 标签。沙箱标签是本次统一构建前镜像，不表示所有历史会话使用该镜像。
- 测试不得调用真实模型；HTTP 验收仅使用明确标记的合成/公开测试输入，逐项恢复开关并删除本次精确测试目标，串行执行以免偏好恢复相互覆盖。

## 验证记录

以下为三方生产代码冻结后的合并版本结果；专项与全量存在覆盖重叠，不重复累计为测试总数。

| 验证层 | 最终结果 |
| --- | --- |
| 前端 | 类型检查、生产构建通过；2,257 项测试通过，0 跳过 |
| Cordis 与协议 | 71 项通过；批准规范及生成副本一致，73 个适配器、74 个插件 |
| 后端完整测试 | 最终镜像依赖环境下 4,743 通过、32 条件跳过、151 个子测试通过 |
| 沙箱完整测试 | 最终镜像依赖环境下 2,787 通过、0 跳过、449 个子测试通过；严格开启科学参考验证 |
| 真实 Chrome | 66/66：44 个旧场景、本批 8 个场景、main 迁移 14 个场景；无外联/意外请求或控制台错误，卸载清理通过 |
| 第五批真实隔离 worker | 15/15：11 正例、4 预期拒绝；使用最终镜像内实际生产代码 |
| 原生兼容专项 | 362 项与 32 个子测试通过；新旧库两种加载顺序均通过 GRIB、HDF5/NetCDF4、CZI 实际读写验证 |
| 验收脚本清理安全专项 | 22 项通过；精确测试目标绑定、响应不确定时恢复、拒绝越界清理 |
| 正式本机服务 | 页面及目录 API 200；Compose 配置有效；无第二套服务或新增公网入口 |

32 个后端跳过项不计为通过，未通过真实模型调用补测。科学参考库仅安装在一次性测试容器中，没有把这批测试依赖加入生产镜像。已有构建大分块提示及测试警告仍存在，不代表已完成所有性能优化或消除全部警告。

### 部署后真实 HTTP

全部通过既有 `http://frontend` 入口串行执行；每个脚本成功退出后才运行下一个，没有并行切换插件状态。

| 脚本 | 已记录检查 | 临时文件上传 / 删除 |
| --- | --- | --- |
| `check_domain_batch_five_http.py` | 45：11 正常、34 拒绝 | 9 / 9 |
| `check_main_migration_visualization_http.py` | 100：32 正常、68 拒绝 | 20 / 20 |
| `check_domain_batch_four_http.py` | 75：18 正常、57 拒绝 | 13 / 13 |
| `check_domain_batch_three_http.py` | 36：11 正常、25 拒绝 | 9 / 9 |
| `check_domain_batch_two_http.py` | 29：7 正常、22 拒绝 | 8 / 8 |
| `check_domain_visualization_http.py` | 29：11 正常、18 拒绝 | 8 / 8 |
| `check_batch_three_visualization_http.py` | 38：9 正常、29 拒绝 | 7 / 7 |
| `check_visualization_http.py` | 12：7 预览、5 拒绝 | 4 / 4 |
| `check_extended_visualization_http.py` | 35：15 目录/预览/旧接口、20 拒绝；另有 4 项 QC 流程检查 | 14 / 14 |

合计九组、399 项已记录检查，另有 4 项 QC 流程检查；这是脚本检查记录数，不是 HTTP 请求总数。覆盖旧 CSV/文本/下载、NetCDF/FITS/FASTQ、科学分块与领域窗口，以及 QC 完成、取消、停用后仍可取消等流程。

92 个临时文件全部清理；同时删除本轮准确归属的 2 个 QC 作业、1 个 QC 产物，均为测试生成数据，不涉及用户数据，无需恢复。九组均 `passed=true`、`business_state_preserved=true`、`cleanup_errors=[]`；测试期间切换的插件偏好和临时所有者全部恢复。验收脚本未调用 Agent/模型，未创建业务会话。没有遗留本轮 fixture 容器。

### 业务状态与运行容器的核对范围

发布前业务摘要：169 个文件、30 个会话、1867 条事件、478 条模型记录、468 条 Token 记录、51 个数据集；当时运行会话和活动分析作业均为 0。数字仅用于核对，不把并行用户正常操作视为测试损坏，也不声称数据库逐字节不变。

实际切换前再次确认活动会话/作业均为 0。九组 HTTP 各自的验收前后摘要相等；最后一组还验证了已有 203 个 AnalysisJob、2 个 Spill Artifact 的数量和摘要不变。摘要范围是记录数量、会话关联状态和事件/模型/Token 元数据，不是所有文件内容及整个数据库的字节级证明。

验收结束后的另一次只读全局快照出现 31 个会话、1871 条事件、480 条模型记录、469 条 Token 记录和 1 个活动会话，文件/数据集仍为 169/51。该活动的发起方未核实，不能将全局新增记录误算为验收脚本调用，也不能宣称发布后所有业务计数一直不变。未停止、删除或回滚这些活动。

协调发布仅主动替换前后端；MongoDB、Redis、MinIO、Office 网关和 ONLYOFFICE 的实例 ID 与启动时间保持发布前值。没有执行旧任务 sandbox/Jupyter 的重启或删除。验收后清单观察到旧任务容器不再列出、新任务容器出现，其原因未核实，不据此声称所有历史容器始终不变。新实际任务沙箱 `ai-dataseek-sandbox-2b63b75d` 的镜像 ID 已只读确认与本次最终沙箱一致。

## 发布镜像与一致性

| 组件 | 最终镜像 ID |
| --- | --- |
| backend | `sha256:4ccff60ea35d92b0f17d876417ce0d403e265fbc5357d29c05d8f7479af8063f` |
| frontend | `sha256:615635eb9d89a02b3f2a27ddc0791ccc1189fdd4a815d7d6939bdae15f95c242` |
| sandbox | `sha256:856b09329cc1dc5360c13a5fe0c2abe0fd3bc4473b8742688521bdd5d3f91543` |

- 冻结清单包含 1,058 个生产源码/配置文件；构建前、切换前、验收后摘要一致：`255b36accf7ed3c953186f2334968dd566042ed458602e35600ba53f8d0d5eab`。文档与测试不计入这份生产清单。
- 镜像内 400 个后端 Python 文件、90 个沙箱 Python 文件与冻结源文件逐项一致；运行后端的全部 435 个清单内 app 文件亦逐项一致。
- 正式前后端容器的 `.Image` 与上表相同；沙箱预热池仍为 0，新任务配置为 `ai-dataseek-sandbox:latest`。
- 回退标签 `ai-dataseek-{backend,frontend,sandbox}:pre-unified-20260911` 已保留，未执行回退。沙箱回退标签仅指统一构建前的镜像，不代表所有历史会话镜像。
- 未提交或推送 Git，当前分支仍为 `v2`。

## 证据与后续边界

本机完整日志及摘要目录：`/private/tmp/dataseek-unified-release.UzY5kU`，包括 `http-summary.json`、九组 HTTP 日志、最终全量测试日志、构建日志、冻结清单及镜像摘要。真实原生检查在 `/private/tmp/dataseek-final-instrument.Lq2MQr`。这些临时目录可能被系统清理；本文件保留最终结果、发布标识和复测入口。

Chrome 最终报告分别为临时目录 `dataseek-visualization-browser-JiYPTJ/results.json`（52 场景）及 `dataseek-visualization-browser-VnpWkx/results.json`（14 场景），不引用开发过程中的失败报告作为最终验收。

本轮没有扩大受限格式支持范围、执行真实模型收费评测或保证所有未知文件方言可用。已有工具默认视图/启停偏好和科学语义边界保留。新增能力与限制见[第五批记录](changes-2026-09-11-domain-visualization-batch-five.md)、[main 可视化迁移](changes-2026-09-11-main-visualization-migration.md)和[领域扩展路线](domain-visualization-expansion-roadmap.md)。
